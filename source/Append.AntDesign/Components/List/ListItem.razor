@namespace Append.AntDesign.Components
@inherits AntComponent

<li class="@classes" style="@Style">
    @if (Actions.Count != 0 || Extra != null)
    {
        @GenerateMainContent();
    }
    else
    {
        @ChildContent
    }
</li>